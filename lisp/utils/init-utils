;;; init-util  --- summary  -*-Emacs-lisp-*-

;; Copyright (C) 2020 Mr.Revolt

;; Author: Mr.Revolt
;; Keywords: faster

;;; Commentary:
;; Emacs package configuration

;;; Code:

(add-to-list 'load-path "~/.emacs.d/lisp/utils/")

;; evil mode
(require 'init-evil "./evil/init-evil")

;; helm mode
(require 'init-helm"./helm/init-helm")

;; require command log mode when emacs startup
(require 'init-command-log "./command-log/init-command-log")

;; hunspell & ispell
(require 'init-fly-spell "./flyspell/init-fly-spell")

;; projectile mode
(require 'init-projectile "./projectile/init-projectile")

;; ox-hugo exporter
(require 'init-hugo "./hugo/init-hugo")

;; mode line
(require 'init-mode-line "./mode-line/init-mode-line")

;; yasnippet
(require 'init-yasnippet "./yasnippet/init-yasnippet")

;; circe --- the IRC inhance client in emacs
(require 'init-circe "./circe/init-circe")

;; wolfram --- a kind of math program language,but run script export result need latex support
;;(require 'init-wolfram "./wolfram/init-wolfram")

;;; latex --- latex need to more configuration,so we must slowly step by step test.
;; 1.How to create LaTex documents with Emacs
;; Link: https://opensource.com/article/18/4/how-create-latex-documents-emacs

;; slime-mode: Superior lisp interaction mode for Emacs
(require 'init-slime "./slime/init-slime")

;; auto-save-mode
(require 'init-real-auto-save "./real-auto-save/init-real-auto-save")

;; pdf-tools - in emacs view / annation pdf file
(require 'init-pdf-tools "./pdftools/init-pdf-tools")

;; chart-tools - in emacs use meimaid make flows image.
;;(require 'init-mermaid "./mermaid/init-mermaid")

;; html support
(require 'init-web-mode "./web/init-web-mode")

;; web-beaufify
(require 'init-web-beautify "./web-beautify/init-web-beautify")

;; web emmet
(require 'init-emmet "./emmet/init-emmet")

;; tern
(require 'init-tern "./tern/init-tern")

;; company mode
(require 'init-company "./company/init-company")

;; neotree
(require 'init-neotree "./neotree/init-neotree")

;; all-the-icons
(require 'init-all-the-icons "./all-the-icons/init-all-the-icons")

;; support r markdown
(require 'init-poly "./poly/init-poly")

;; posframe support
(require 'init-posframe "./posframe/init-posframe")

(require 'init-sdcv"./sdcv/init-sdcv")

(require 'init-engine "./engine/init-engine")

(require 'init-company-english-helper"./company-english-helper/init-company-english-helper")

(require 'init-insert-translated-name "./insert-translated-name/init-insert-translated-name")

(require 'init-expand-region "./expand-region/init-expand-region")

;; chinese input methods: pyim
(require 'init-pyim "./pyim/init-pyim")

;;  java-script IDE
(require 'init-js2-mode "./js2-mode/init-js2-mode")

;; Emacs window manager tools
(require 'init-window "./window/init-window")

;; Support golang program language.
(require 'init-golang "./golang/init-golang")

;; Use Emacs client for an LSP Server.
(require 'init-lsp-mode "./lsp-mode/init-lsp-mode")

(require 'init-flymake "./flymake/init-flymake")

(provide 'init-utils)

;;; init-utils ends here
